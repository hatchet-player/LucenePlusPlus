# test.analysis.standard build file
#========================================================================

test_analysis_standard_dir = meson.current_source_dir()

# src
#========================================================================


test_src += files(
    join_paths(test_analysis_standard_dir, 'StandardAnalyzerTest.cpp'))
