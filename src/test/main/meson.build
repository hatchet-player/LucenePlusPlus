# test.main build file
#========================================================================

test_main_dir = meson.current_source_dir()

# src
#========================================================================


test_src += files(
    join_paths(test_main_dir, 'main.cpp'))
    