# test.search.payloads build file
#========================================================================

test_search_payloads = meson.current_source_dir()

# src
#========================================================================

test_src += files(
    join_paths(test_search_payloads, 'PayloadHelper.cpp'),
    join_paths(test_search_payloads, 'PayloadNearQueryTest.cpp'),
    join_paths(test_search_payloads, 'PayloadTermQueryTest.cpp'))
