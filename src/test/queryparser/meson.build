# test.queryparser build file
#========================================================================

test_queryparser_dir = meson.current_source_dir()

# src
#========================================================================


test_src += files(
    join_paths(test_queryparser_dir, 'MultiAnalyzerTest.cpp'),
    join_paths(test_queryparser_dir, 'MultiFieldQueryParserTest.cpp'),
    join_paths(test_queryparser_dir, 'QueryParserTest.cpp'))
    