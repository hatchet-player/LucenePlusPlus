# test.contrib.memory build file
#========================================================================

test_contrib_memory_dir = meson.current_source_dir()

# src
#========================================================================


test_src += files(
    join_paths(test_contrib_memory_dir, 'MemoryIndexTest.cpp'))
