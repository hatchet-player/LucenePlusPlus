# dot Dependency File
#========================================================================


# find dot
#---------------------------------------------------
dot_bin = find_program(
    'dot',
    required : false)


if (not dot_bin.found())
    dot_bin = disabler()
endif
