# test.msvc build file
#========================================================================

test_msvc_dir = meson.current_source_dir()

# src
#========================================================================


test_src += files(
    join_paths(test_msvc_dir, 'TestInc.cpp'))
    