# test.search.spans build file
#========================================================================

test_search_spans = meson.current_source_dir()

# src
#========================================================================

test_src += files(
    join_paths(test_search_spans, 'BasicSpansTest.cpp'),
    join_paths(test_search_spans, 'FieldMaskingSpanQueryTest.cpp'),
    join_paths(test_search_spans, 'NearSpansOrderedTest.cpp'),
    join_paths(test_search_spans, 'PayloadSpansTest.cpp'),
    join_paths(test_search_spans, 'SpanExplanationsTest.cpp'),
    join_paths(test_search_spans, 'SpansAdvanced2Test.cpp'),
    join_paths(test_search_spans, 'SpansAdvancedTest.cpp'),
    join_paths(test_search_spans, 'SpansTest.cpp'))
    