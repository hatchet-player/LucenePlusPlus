# test.analysis.tokenattributes build file
#========================================================================

test_document_dir = meson.current_source_dir()

# src
#========================================================================


test_src += files(
    join_paths(test_document_dir, 'BinaryDocumentTest.cpp'),
    join_paths(test_document_dir, 'DateFieldTest.cpp'),
    join_paths(test_document_dir, 'DateToolsTest.cpp'),
    join_paths(test_document_dir, 'DocumentTest.cpp'),
    join_paths(test_document_dir, 'NumberToolsTest.cpp'))
    