# liblucene.util.unicode build file
#========================================================================


core_inc += include_directories('.')
liblucene_util_unicode_dir = meson.current_source_dir()


core_src += files(
    join_paths(liblucene_util_unicode_dir, 'guniprop.cpp'))
    